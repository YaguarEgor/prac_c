#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);
    unsigned long X[N];
    unsigned long S;
    for (int i = 0; i < N; i++) {
        scanf("%x", &X[i]);
    }
    scanf("%d", &S);
    
}