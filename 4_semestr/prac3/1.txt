program {
    int a = 51, b, c;
    bool abc = true, g = true;
    string x = "abc", z = "abcd", y = "h";
    c = (a + b) * 2;
    a = c = b;
    2;
    if (c >= 100 or x == z) {
        read (y);
        write (y);
        write (x + y + z, c);
    } else {
        write(6);
    }

    
    
    while (abc) {
        c = c - 5;
        write (c);
        x = x + "step";
        break;
    }

    goto m;
    1+2;

    m:
    write (x);

    {/* this is a comment */ }
}