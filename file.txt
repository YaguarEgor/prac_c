program {
    int a = 51, b = 6, c;
    string x = "abc", y, z = "abcd";
    c = (a + b) * 2;
    if (c >= 100 or x == z) {
        read (y);
        write (y);
        write (x + y + z, c);
    }
    while (abc) {
        c = c - 5;
        write (c);
        x = x + "step";
    }
    break;
    write (x);

    {/* this is a comment */ }
}